export class User {
    constructor(
        private email: String,
        private crDate: String,
        private role: String,
        private status: String,
        private name: String,
        private image: String
    ) { }
}